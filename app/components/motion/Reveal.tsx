// Alias histórico: Reveal y SectionWrapper eran el mismo componente
// duplicado en dos archivos. Se unifica en SectionWrapper.tsx (componente
// "AnimatedSection" de la sección 5.2 del CLAUDE.md).
export { SectionWrapper as Reveal } from "../SectionWrapper";
